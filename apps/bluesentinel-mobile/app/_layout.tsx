// apps/bluesentinel-mobile/app/_layout.tsx
// Root layout for BlueSentinel Mobile (Expo Router)
// Phase 4 scaffold placeholder — full implementation in Phase 6+
import { Slot } from "expo-router";

export default function RootLayout() {
  return <Slot />;
}
