Marine Guardian Enterprise — Phase 4 Scaffold MANIFEST
Generated: 2026-03-15
Agent: CLINE

═══════════════════════════════════════════════════════════════
MONOREPO STRUCTURE
═══════════════════════════════════════════════════════════════

Root
├── package.json                  — Monorepo root, turbo orchestration
├── pnpm-workspace.yaml           — pnpm workspace definition
├── turbo.json                    — Turborepo pipeline config
├── tsconfig.base.json            — Shared TS config (strict, ESM)
├── .eslintrc.js                  — Shared ESLint rules (strict TS)
├── .prettierrc                   — Code formatting rules
├── .editorconfig                 — Editor whitespace rules
├── inputs.yml                    — Agent-owned spec (derived from PRODUCT.md)
├── inputs.schema.json            — JSON Schema for inputs.yml validation
└── MANIFEST.txt                  — This file

packages/
├── shared/                       — TypeScript interfaces + Zod schemas
│   └── src/
│       ├── enums.ts              — All platform enums (Role, TenantType, etc.)
│       ├── types/                — Pure TS interfaces (no runtime deps)
│       │   ├── tenant.ts
│       │   ├── user.ts
│       │   ├── fisherfolk.ts
│       │   ├── vessel.ts
│       │   ├── permit.ts
│       │   ├── catch-report.ts
│       │   ├── program.ts
│       │   ├── incident.ts
│       │   ├── patrol.ts
│       │   ├── notification.ts
│       │   ├── audit-log.ts
│       │   └── index.ts
│       ├── schemas/              — Zod validation schemas
│       │   ├── fisherfolk.schema.ts
│       │   ├── vessel.schema.ts
│       │   ├── permit.schema.ts
│       │   ├── catch-report.schema.ts
│       │   ├── program.schema.ts
│       │   ├── incident.schema.ts
│       │   ├── patrol.schema.ts
│       │   ├── tenant.schema.ts  — Login/CreateUser/MobileAuth schemas
│       │   └── index.ts
│       └── index.ts              — Public barrel export

├── db/                           — Prisma ORM + security layers
│   ├── prisma/
│   │   ├── schema.prisma         — Full 18-model Prisma schema
│   │   └── migrations/
│   │       └── apply_rls_policies.sql — PostgreSQL RLS + roles
│   └── src/
│       ├── client.ts             — PrismaClient singleton
│       ├── rls.ts                — L2: withTenantContext (SET LOCAL)
│       ├── audit.ts              — L5: Immutable AuditLog writer
│       ├── middleware/
│       │   └── tenant-guard.ts   — L6: tenantId write guard
│       ├── repositories/
│       │   ├── fisherfolk.repo.ts
│       │   ├── permit.repo.ts
│       │   └── tenant.repo.ts
│       ├── seed.ts               — Dev data: BA + Calapan + 62 barangays
│       └── index.ts

├── api-client/                   — tRPC client factory (web + mobile)
│   └── src/
│       ├── client.ts             — createApiClient() factory
│       ├── router-type.ts        — AppRouter type bridge
│       └── index.ts

├── ui/                           — shadcn/ui components + Tailwind
│   └── src/
│       ├── lib/utils.ts          — cn() helper
│       ├── components/
│       │   ├── button.tsx
│       │   ├── badge.tsx
│       │   ├── card.tsx
│       │   └── input.tsx
│       └── index.ts

├── jobs/                         — BullMQ queue + workers
│   └── src/
│       ├── redis.ts              — IORedis connection singleton
│       ├── queues.ts             — 7 queues + job data types
│       ├── workers/
│       │   ├── permit-expiry.worker.ts      — Daily permit expiry cron
│       │   └── notification-dispatch.worker.ts — Push/Email/In-app
│       ├── dlq/
│       │   └── dlq-helpers.ts    — DLQ inspection + retry/dismiss
│       └── index.ts

└── storage/                      — S3-compatible storage wrapper
    └── src/
        ├── client.ts             — S3Client singleton (MinIO/S3/R2)
        ├── buckets.ts            — StoragePaths + presigned URL helpers
        └── index.ts

apps/
├── marine-guardian-enterprise/   — Next.js 15 web admin
│   ├── package.json
│   ├── next.config.ts
│   ├── tsconfig.json
│   ├── tailwind.config.ts
│   ├── postcss.config.mjs
│   └── src/app/
│       ├── layout.tsx
│       ├── page.tsx
│       ├── globals.css
│       └── api/health/route.ts   — GET /api/health → DB ping

├── bluesentinel-mobile/          — Expo 52 mobile app
│   ├── package.json
│   ├── app.json
│   └── tsconfig.json

└── worker/                       — BullMQ worker process
    ├── package.json
    ├── tsconfig.json
    └── src/index.ts

tools/
├── validate-inputs.mjs           — inputs.yml schema validation
└── check-env.mjs                 — Required env var pre-flight check

deploy/
├── compose/
│   └── docker-compose.yml        — Postgres16 + Valkey8 + MinIO + web + worker
└── docker/
    ├── Dockerfile.web            — Multi-stage Next.js image
    └── Dockerfile.worker         — Multi-stage worker image

.github/workflows/
└── ci.yml                        — TypeCheck + Lint + Build + Prisma validate

═══════════════════════════════════════════════════════════════
SECURITY LAYERS
═══════════════════════════════════════════════════════════════
L1  Auth.js (next-auth v5) — session tokens, CSRF
L2  RLS withTenantContext — SET LOCAL per transaction
L3  tRPC procedure guards — role + tenant membership check
L4  Zod input validation — all mutations validated
L5  Immutable AuditLog — write-only, no delete/update by mg_app
L6  Prisma TenantGuard middleware — missing tenantId = hard error
L7  RBAC role matrix — enforced in tRPC procedures

═══════════════════════════════════════════════════════════════
DATABASE MODELS (18)
═══════════════════════════════════════════════════════════════
Tenant, User, TenantMembership, Barangay, Species,
Fisherfolk, Vessel, Permit, CatchReport, Program,
ProgramBeneficiary, DistributionEvent, Incident, Patrol,
Notification, AuditLog, PushToken, RefreshToken

═══════════════════════════════════════════════════════════════
QUEUES (7)
═══════════════════════════════════════════════════════════════
image-optimization, permit-pdf, idcard-pdf,
permit-expiry (cron daily), notification-dispatch,
offline-sync, evidence-photo
